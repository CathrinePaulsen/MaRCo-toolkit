The `reproducible-central` directory has been intentionally omitted due to the lack of an explicit copyright license 
in the original source material.

The original source material can be obtained from the GitHub repository `jvm-repo-rebuild/reproducible-central` 
at commit `e5e03d8d9337c1f99a889431cf6a294bb3fd0387`.

To download the missing content, run the `download_rc.sh` script in the root `evaluatelinker` directory.